{
  "//": "The toolkits, which run as Workers. `types: []` is a deliberate guard — a Worker has no `process` and no `node:fs` — and node-shaped tooling lives in its own project. Shared byte for byte with the other catalog.",
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "skipLibCheck": true,
    "target": "es2022",
    "module": "esnext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "lib": ["es2022", "dom"],
    "types": [],
    "noEmit": true
  },
  "include": ["**/*.ts"]
}
